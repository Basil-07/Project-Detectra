# Replace this value in every non-local environment.
DETECTRA_SECRET_KEY=replace-with-a-long-random-value

# Vercel only: public URL of detectra-models.zip from a GitHub Release.
DETECTRA_MODELS_URL=

# Optional but recommended SHA256 printed by package_models.
DETECTRA_MODELS_SHA256=
